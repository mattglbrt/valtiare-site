---
interface Props {
  minutes: number;
}
const { minutes } = Astro.props;
const display = `${Math.max(1, Math.round(minutes))} min read`;
---

<span class="font-sans text-xs uppercase tracking-[0.18em] text-stone-500">
  {display}
</span>
